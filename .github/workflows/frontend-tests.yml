name: frontend-tests

on:
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Frontend Test and Lint
        run: |
          cd frontend
          npm ci
          npm run lint -- --no-fix
